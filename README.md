# Django Threat Monitoring Backend

## 📌 Overview
This project is a backend system built using **Django** and **Django REST Framework** that ingests security events from external systems (such as firewalls or intrusion detection systems) and automatically generates alerts based on event severity.

The application is designed as part of a backend assignment to demonstrate clean API design, ORM usage, and business logic separation.

---

## 🚀 Features
- REST API to ingest security events
- Automatic alert generation for **High** and **Critical** severity events
- Modular Django app structure
- Django Admin panel support
- SQLite database (default)
- Clean separation of concerns using services

---

## 🛠 Tech Stack
- Python 3
- Django
- Django REST Framework
- SQLite

---

## 📂 Project Structure


